<section class="card">
  <a href="/blog/{{id}}">
    <header>{{title}}</header>

    <div class="card-body">
      <img src="/blog/{{filename}}" alt="{{description}}">
      <p>By: {{artist}}</p>
    </div>
  </a>
</section>